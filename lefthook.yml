pre-commit:
  commands:
    lint:
      glob: "app/*.{js,ts,jsx,tsx}"
      run: pnpm biome check {staged_files}
    format:
      glob: "app/*.{js,ts,jsx,tsx}"
      run: pnpm biome format {staged_files} --write
